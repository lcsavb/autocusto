upstream django {
    server web:8001;
}

server {
    listen 80;

    location /static/ {
        alias /home/appuser/app/static_root/;
    }

    location / {
        uwsgi_pass django;
        include uwsgi_params;
    }
}

types {
    application/pdf pdf;
}
